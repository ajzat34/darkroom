# version 300 es
  // generated by OpenDarkroom/tools/nlmeans
  // impliments fast nlmeans filter in glsl
  uniform sampler2D texSampler;
  uniform ivec2 size;
  uniform sampler2D weights;
  in highp vec2 textureCoord;
  out highp vec4 fragmentColor;
  void main(void){
 highp ivec2 p = ivec2(int(textureCoord.x * float(size.x)), int(textureCoord.y * float(size.y)));
 highp vec3 acc = vec3(0.0);
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-64, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(16,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-63, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-62, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-61, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-60, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-59, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-58, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-57, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-56, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-55, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-54, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-53, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-52, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-51, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-50, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-49, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-48, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-47, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-46, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-45, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-44, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-43, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-42, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-41, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-40, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-39, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-38, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-37, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-36, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-35, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-34, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-33, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-32, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-31, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-30, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-29, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-28, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-27, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-26, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-25, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-24, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-23, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-22, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-21, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-20, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-19, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-18, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-17, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-16, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-15, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-14, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-13, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-12, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-11, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-10, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-9, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-8, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-7, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-6, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-5, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-4, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-3, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-2, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+-1, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+0, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+1, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+2, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+3, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(0,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+4, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+5, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+6, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+7, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(1,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+8, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+9, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+10, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+11, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(2,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+12, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+13, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+14, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+15, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(3,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+16, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+17, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+18, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+19, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(4,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+20, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+21, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+22, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+23, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(5,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+24, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+25, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+26, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+27, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(6,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+28, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+29, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+30, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+31, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(7,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+32, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+33, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+34, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+35, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(8,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+36, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+37, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+38, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+39, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(9,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+40, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+41, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+42, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+43, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(10,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+44, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+45, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+46, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+47, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(11,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+48, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+49, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+50, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+51, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(12,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+52, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+53, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+54, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+55, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(13,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+56, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+57, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+58, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+59, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(14,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+60, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).r;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+61, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).g;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+62, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).b;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+63, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(15,0), 0).a;
 acc+= texelFetch(texSampler, ivec2(clamp(p.x, 0, size.x-1), clamp(p.y+64, 0, size.y-1)), 0).rgb * texelFetch(weights, ivec2(16,0), 0).r;

  fragmentColor.rgb = acc;
  fragmentColor.a  = 1.0;}
